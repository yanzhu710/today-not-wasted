# Changelog

## v2.5.1
- 伙伴页重新收束：陪伴值、当前阶段、下一阶段距离和四级进度全部并入伙伴主场景；伙伴页顶部取消透明粘滞叠层，修复滚动时标题、形态/闪卡切换与顶部操作互相重叠。
- 伙伴静默时会在场景内轻微来回走动；睡觉或互动时自动暂停。八类动作改为当前阶段资源预加载 + 双图层交叉淡入，减少喂食、摸摸、玩耍、鼓励切图时的白闪。
- 阶段进化特效升级：加入正式阶段形态、光环、旋转环、粒子、震动和更强的入场动画；Lv.4 加冕使用独立金色仪式效果。
- 四阶 GLB 闪卡修正材质过曝：按 sRGB/线性空间处理卡面纹理、降低 emissive 和叠加高光，调整模型距离；静止时加入真实模型的轻微呼吸、上下漂浮与视角微动。
- 伙伴主场景和今天页伙伴卡改用栅格美术背景，不再依赖 SVG/CSS 拼接场景；伙伴位置、气泡、状态与信息层重新布局。
- 伙伴轻状态改为表情贴纸。用户自己的今日心情同步表情化：首页“心情”入口、伙伴场景角标、今日总览、记录时间轴、心情列表和心情选择器都展示对应表情。
- 保留 v2.5.0 四阶伙伴、陪伴值、偏好、共同回忆、真实 GLB 闪卡、加冕果实、邀请号入口，以及已合并的 v2.4.6 计划/徽章/记账优化。
- Service Worker 与 APP_VERSION 同步升级为 2.5.1，新场景图和 6 张心情表情进入离线缓存；不清空 IndexedDB。

## v2.5.0
- 默认伙伴改为单一完整伙伴体系，用户首次进入时自行命名；内部资源 ID 保持 `ali`，界面不使用固定宠物名。旧用户按最高陪伴值与最早相遇日幂等迁移，不清除 IndexedDB。
- 成长系统由五阶段收束为四阶段：Lv.1 初遇期、Lv.2 熟悉期、Lv.3 亲密期、Lv.4 纪念期；面向用户统一称为“陪伴值”。
- 接入 32 张正式伙伴动作素材（4 阶段 × idle/happy/touch/feed/play/encourage/sleep/celebrate），所有动作按“当前阶段 + 当前动作”取图并带 idle 降级。
- 伙伴主页重构：形态 / 闪卡双视图、当前状态、陪伴值、四类互动、每日奖励上限、偏好发现、伙伴资料、共同回忆与邀请号锁定伙伴入口。
- 四阶闪卡已接入用户提供的四个 GLB 真实 3D 模型：保留模型厚度、内嵌卡面材质与背面材质，并在浏览器实时支持拖动旋转、点击翻面、缩放、自动漂浮、透视深度和高光强度；WebGL 或资源异常时自动退回轻量收藏卡。运行版只包含四个 GLB；原始 Blender 源文件由项目方单独保管，不进入 Pages 部署包。
- 商城新增“成长道具 / 加冕果实”：陪伴值达到 720 才开放兑换；Lv.4 不再自动进入，必须持有并消耗加冕果实完成加冕，随后解锁最终阶段与 Lv.4 闪卡。
- 新增轻状态、喜好发现和独立伙伴回忆数据；普通互动达到每日陪伴值上限后仍可继续互动，但不再无限刷值；不加入饥饿、掉心情或数值衰减。
- 修复 IndexedDB schema v1 → v2 升级路径：升级旧库时使用真实 upgrade transaction 创建新 store，避免旧用户升级时启动失败。
- 一并包含 v2.4.6 的生活灵感 / 徽章页收束、Tab 高亮、记账算式金额与收入分类精简，同时保留 v2.4.5 Android 滚动和窄屏修复。

## v2.4.6
- 修复计划页路由 Tab 点击后内容已切换、视觉高亮却可能停留在旧项的问题：点击时立即同步选中态，路由仍以 URL 为最终状态源。
- 重构“生活灵感”：删除三排时长 / 场景 / 类型 Tab，合并为一个“当前条件 / 筛选”入口；主区域只展示当前推荐，保留“换一个 / 去记录”主操作，收藏和减少推荐收进更多操作。
- 收束徽章页：删除“全部 / 已获得 / 未获得”和“12 系列 / 各系列”两组常驻 Tab，改为单一筛选入口；收藏进度和徽章册直接衔接。
- 记账金额输入支持安全算式计算（+、-、×、÷、*、/、括号），实时显示结果，保存时按计算后的金额入账；不使用 eval。
- 收入分类移除“工资”和“奖金”两个选项；已有历史账目不迁移、不删除，仍按原分类正常显示。
- 数据结构保持兼容；应用与 Service Worker 版本同步升级为 2.4.6，不清空 IndexedDB。

## v2.4.5
- 修复 Android Chrome / WebView 纵向滑动与下拉手势可能失效：根页面不再用 `overflow-x:hidden` 创建额外滚动容器，恢复原生纵向滚动与 overscroll；今天页移除旧的内层纵向滚动容器，弹窗仍保持独立纵向滚动。
- “我的”页面收束为紧凑资料区、四项概览和四个设置入口，显示与声音、备份、更新等内容按需打开，不再纵向堆叠。
- 今天页的记录 / 专注 / 心情 / 记账四项重新并入伙伴主卡并保持单行横排；点击某项后再选择立即操作或查看该类型记录，减少常驻“看记录”按钮占位。
- 计划页取消下拉选择和重复“计划”标题，恢复任务 / 习惯 / 目标 / 生活灵感四项横向切换。
- 今天、接下来、习惯、目标、愿望、记录、背包、回忆等空状态统一为当前伙伴直接融入父区域背景，不再额外生成一张空状态卡片。
- 加强移动端宽度约束：弹窗、日期/时间输入、表单行在窄屏下按可用宽度收缩或单列，重点修复 iPhone Safari 右侧显示不全。
- 声效增加用户手势解锁、回前台恢复、全局普通控件轻反馈和重复音效合并，提高 iOS/Safari 连续播放稳定性。
- 清理启动错误页、设备检查、回顾和快速记录中的开发者口吻，改为面向普通用户的提示。
- 数据结构保持兼容；Service Worker 与应用版本同步升级为 2.4.5，不清空 IndexedDB。

## v2.4.4
- 紧急修复 v2.4.3 中自定义奖励列表脚本缺少一个右括号、浏览器按 ESM 解析报 “missing ) after argument list”，导致进入“伙伴 / 徽章 / 商城”页触发错误边界、页面打不开的问题。
- 同步递增离线缓存版本（Service Worker RELEASE 2.4.3 → 2.4.4）：已安装 v2.4.3 的设备会重新下载并校验全部核心资源，用新缓存替换损坏的旧缓存；不调用 IndexedDB、不清空任何本机记录、宠物、徽章或积分，也不需要手动删除网站数据。
- 说明：代码包自带 link_check 使用 node --check 检查 .js（按脚本模式解析）会漏报该 ESM 语法错误；本次已改用 ESM 解析对全部 38 个模块重新扫描，确认仅此一处，修复后 0 错误。

## v2.4.3
- 修复多处窄屏右侧显示不全：弹窗、时间范围、筛选和操作按钮不再产生横向滚动。
- “我的”重构为资料、积分、显示与声音、数据与备份、应用、关于；显示模式/动画/声音使用下拉选择。
- 伙伴页宠物选择改为等宽布局；“奖励”统一改名“商城”，道具、自定义奖励和我的物品职责重新整理。
- 自定义奖励抠图改为保护主体优先，缩小背景移除范围；提供“保留完整图片”兜底，白边不再强行侵入主体。
- 记录页回顾改为统一周/月回顾中心，旧月度分享卡移除，周/月都调用新版分享面板。
- 今天页四个入口明确区分“新建”和“看记录”；今天宠物场景减少碗和便签等杂乱元素。
- 计划页改为标题 + 下拉选择，生活清单退出主流程；旧路由自动归一，避免内容与高亮不同步。
- 任务、习惯、记录、徽章、背包等奖励空状态使用当前伙伴做引导。
- WebAudio 在 Safari 回前台/页面切换后主动恢复，减少偶发无声。

## v2.4.2
- 记录时间轴按确认稿升级：伙伴插画头图、日期笔刷、纸张卡片、类型色彩、照片拍立得和夜读适配。
- 自定义奖励图片新增本机智能主体识别：从图片边缘估计背景、连通区域抠除、羽化边缘，再生成不规则白色涂鸦边框。
- 透明 PNG 直接沿透明主体描边；复杂背景识别置信度不足时安全降级为白边周边卡，避免误删主体。
- 奖励中心补充制作状态、智能抠图提示、伙伴插画和周边制作氛围。
- 保留 v2.4.1 的四入口记录直达、时间范围、账本分析、窄屏防挤压和固定横向布局。

## v2.4.1
- 全站增加窄屏与横向溢出约束，功能文字优先单行显示，页面仅纵向滚动。
- 今天页四个主入口去掉副标题，手账入口改为“心情”，并增加对应时间轴的快速访问。
- 今日计划合并待办、习惯和目标概览，可直接新建任务与习惯。
- 小事记录和专注确认统一支持“开始时间 → 结束时间”，自动计算实际时长。
- 自定义奖励支持图片上传，自动生成白色手绘贴纸边框，并上架奖励商城兑换。
- 检查更新增加真实阶段式进度条，不使用随机模拟进度。
- 账本新增分类环图、预算进度、六个月趋势和消费洞察；数据页合并相关区块。
- 月历、统计图、弹窗和手账表单限制在视口内，不再左右拖动撑宽页面。

## v2.4.0
- 重构“今天 / 计划 / 记录 / 伙伴 / 我的”的职责边界，减少重复入口。
- 计划页新增任务管理主页；足迹更名为记录并整合时间线、日历、统计与回顾。
- Tab 改为 URL 驱动，修复内容切换后高亮不同步、刷新/返回状态丢失。
- 自定义奖励迁入伙伴的奖励中心，并改为原子兑换，避免不扣积分或重复扣分。
- 手账新增照片选择后预览、高清保存、时间线大图查看；编辑日期会同步时间线事件。
- 今日待办与习惯改为单栏，提升窄屏文字可读性；重做专注视觉和暖色夜读模式。
- 沿用 v2.3.5 的启动保护、PWA 安全缓存和历史数据兼容策略。

## v1.3.0
- 最终上线前精修版：补充“上线前检查”和设备诊断导出，便于发布前自检。
- “我的”页面新增更明确的本机状态、安装能力、备份建议与正式使用提醒。
- 文档与版本号同步更新，PWA 缓存版本升级，便于 GitHub Pages 直接覆盖发布。

## v1.2.0
- 上线前精修：继续统一整体视觉层次与交互反馈。
- 徽章系统增强：已获得徽章支持重复点击彩花、星光与回弹动效。
- 宠物系统增强：宠物渲染细节升级，高阶段家园加入氛围粒子与光环。
- 页面切换动效增强，更新提示条更稳，体验更成熟。
- “我的”页面新增设备与状态区块，便于查看安装状态、数据规模与备份建议。
- 增补 README、CHANGELOG、LICENSE，方便 GitHub 仓库交付。

## v1.1.1
- UI 质感升级。
- 新增安装入口与版本说明。
- GitHub Pages 直传可用。

## v1.0.1
- 初始静态版本。
